# ============================================================
# Linux MCP Server Chatbot - Configuration
# ============================================================
# Copy this file to .env and configure for your environment

# ============================================================
# RECOMMENDED: Claude via Google Vertex AI
# ============================================================
# Uses the SAME authentication as Claude Code CLI!
# - No API keys needed
# - Uses Google Cloud Application Default Credentials
# - Costs billed to your GCP organization
# - Same model: Claude Sonnet 4.5
# - Fast responses: 2-3 seconds

MODEL_ENDPOINT=https://vertex-ai-anthropic
MODEL_NAME=claude-sonnet-4-5@20250929
GOOGLE_PROJECT_ID=your-gcp-project-id
GOOGLE_LOCATION=us-east5

# How to find your values:
# - GOOGLE_PROJECT_ID: Run `gcloud config get-value project`
# - Authenticate: Run `gcloud auth application-default login`

# Available regions for Claude on Vertex AI:
# - us-east5 (tested and working)
# - europe-west1 (may work)
# - asia-southeast1 (may work)
# Note: us-central1 does NOT support Claude

# ============================================================
# Alternative: Direct Claude API (Anthropic)
# ============================================================
# Requires API key from https://console.anthropic.com/
# MODEL_ENDPOINT=https://api.anthropic.com
# MODEL_NAME=claude-sonnet-4-20250514
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# ============================================================
# Alternative: Gemini via AI Studio
# ============================================================
# Requires API key from https://aistudio.google.com/app/apikey
# Note: May be blocked by organization policies
# MODEL_ENDPOINT=https://genai
# MODEL_NAME=gemini-1.5-pro
# GOOGLE_API_KEY=your-api-key-here

# ============================================================
# Alternative: Gemini via Vertex AI
# ============================================================
# Uses Google Cloud credentials (same as Claude Vertex)
# MODEL_ENDPOINT=https://vertex-ai
# MODEL_NAME=gemini-1.5-pro
# GOOGLE_PROJECT_ID=your-gcp-project-id
# GOOGLE_LOCATION=us-central1

# ============================================================
# Alternative: Local Models (Ollama, vLLM, KServe, etc.)
# ============================================================
# OpenAI-compatible model endpoint
# MODEL_ENDPOINT=http://localhost:8080
# MODEL_NAME=mistral-small
#
# For Ollama: Leave MODEL_NAME empty to auto-detect models
# For vLLM: Must specify exact model name
#
# KServe-specific:
# OPENAI_API_PATH=openai
# OPENAI_API_HOST=model-predictor.namespace.example.com

# ============================================================
# Linux MCP Server Configuration
# ============================================================
# Path to linux-mcp-server binary
# Find with: which linux-mcp-server
MCP_COMMAND=/home/user/.local/bin/linux-mcp-server

# SSH username for remote Linux hosts
# This is the username you use to SSH to remote machines
# Example: If you run `ssh localuser@demo.example.local`, use:
LINUX_MCP_USER=localuser

# ============================================================
# Advanced Settings (Optional)
# ============================================================
# API request timeout in seconds
REQUEST_TIMEOUT=300

# Tool choice mode
# - "none": Default, works with most servers
# - "auto": Requires server support (vLLM --enable-auto-tool-choice)
# - "required": Forces tool use
TOOL_CHOICE=none

# Model context window size (tokens)
# MODEL_CONTEXT_TOKENS=4096

# Maximum characters per tool output
# Prevents context overflow
# MAX_TOOL_OUTPUT_CHARS=2400

# ============================================================
# Performance Tips
# ============================================================
# 1. Use Claude via Vertex AI for best performance (2-3 sec responses)
# 2. Configure SSH ControlMaster for 30x faster remote queries:
#    mkdir -p ~/.ssh/controlmasters
#    Add to ~/.ssh/config:
#      Host *
#          ControlMaster auto
#          ControlPath ~/.ssh/controlmasters/%r@%h:%p
#          ControlPersist 10m
# 3. Set up SSH keys to avoid password prompts
# 4. Use specific hosts in SSH config instead of wildcards

# ============================================================
# Verification
# ============================================================
# Test your setup:
# 1. Run: python tests/test_setup.py
# 2. Run: python tests/test_mcp_direct.py
# 3. Start: ./start-chatbot.sh
