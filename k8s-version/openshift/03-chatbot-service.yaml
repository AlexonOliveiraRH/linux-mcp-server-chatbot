apiVersion: v1
kind: Service
metadata:
  name: linux-mcp-chatbot
  namespace: linux-mcp-chatbot
  labels:
    app: linux-mcp-chatbot
spec:
  type: ClusterIP
  selector:
    app: linux-mcp-chatbot
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
