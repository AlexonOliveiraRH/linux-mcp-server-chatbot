apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linux-mcp-chatbot

resources:
- namespace.yaml
- serviceaccount.yaml
- configmap.yaml
- secret.yaml
- deployment.yaml
- service.yaml

# Optional: Uncomment based on your platform
# - route.yaml        # For OpenShift
# - ingress.yaml      # For Kubernetes

commonLabels:
  app: linux-mcp-chatbot
  version: v1.0.0

images:
- name: quay.io/your-org/linux-mcp-chatbot
  newTag: latest
